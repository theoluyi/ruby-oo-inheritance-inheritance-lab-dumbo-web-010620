class User
  # create getter+setter methods for instances
  attr_accessor :first_name, :last_name
end

# so this was breaking it because this is more than what they asked for
# define the constructor/initialization process for a new instance
# def initialize(first_name, last_name)
#   @first_name = first_name
#   @last_name = last_name
# end
